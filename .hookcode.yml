version: 1

dependency:
  failureMode: soft
  runtimes:
    - language: node
      install: "pnpm install --frozen-lockfile"

preview:
  instances:
    - name: app
      command: "pnpm dev -- --port {{PORT}}"
      workdir: "frontend"
      env:
        PORT: "{{PORT}}"